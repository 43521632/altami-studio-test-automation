# Инструкция для AI — проект автотестов Altami Studio

## 1. Цель

Переделать текущие тесты так, чтобы они были **изолированы друг от друга**. Каждый тест должен запускаться из гарантированно чистого состояния приложения (не ВМ).

**Архитектура:**
1. **Однократная подготовка ВМ** (до всех тестов): восстановление снапшота, получение установщика, запуск ВМ.
2. **Один тест установки** — устанавливает приложение, выполняет первичную настройку, закрывает приложение.
3. **Все остальные тесты** — запускают приложение, проверяют, закрывают. Каждый тест начинается с чистого запуска и не влияет на другие.

**Источники данных:**
- Ревизия сборки — из CI (переменная `BUILD_REVISION`), аргумент `--revision` или **из Kiwi TCMS** (через API).
- Версия продукта — из CI (`APP_VERSION`), аргумента `--app-version` или файла `VERSION` в корне проекта.
- Установщик — скачивается с `https://cache.altami.ru/artifactory/app-builds/altami/{revision}/{platform}/AS_{version}_{revision}_{platform}.{ext}`.
- Аутентификация — логин/пароль из `CACHE_USER`/`CACHE_PASSWORD`.

---

## 2. Интеграция с Kiwi TCMS

### 2.1. Общая концепция

- **Тест-раны создаются вручную** в Kiwi TCMS (через UI). Автотесты их не создают.
- **Каждый тест в коде сопоставлен с ID кейса** в Kiwi через `src/case_ids.py`.
- **Автотесты обновляют существующие тест-раны**: добавляют результаты для каждого кейса и прикрепляют артефакты (лог, скриншоты, diff).
- **По завершении тест-ран автоматически закрывается** (опция).

### 2.2. Реализованные компоненты

| Файл | Назначение | Статус |
|------|------------|--------|
| `utils/kiwi_client.py` | Клиент для API Kiwi TCMS (аутентификация, получение ревизии, отправка результатов) | ✅ **Реализован полностью** (через `tcms-api`) |
| `utils/pytest_kiwi_plugin.py` | Плагин pytest для сбора результатов и отправки в Kiwi | ✅ Готов |
| `utils/config.py` | Получение ревизии из Kiwi (приоритет: BUILD_REVISION → --revision → Kiwi) | ✅ Готов |
| `tests/conftest.py` | Подключение плагина `pytest_kiwi_plugin` | ✅ Готов |

### 2.3. Переменные окружения для Kiwi

| Переменная | Назначение | Обязательность |
|------------|------------|----------------|
| `KIWI_URL` | URL Kiwi TCMS (например, `https://kiwitcms.altami.ru`) | Да |
| `KIWI_USER` | Логин (используется для аутентификации и проверки назначения) | Да |
| `KIWI_PASSWORD` | Пароль | Да |
| `KIWI_TESTRUN_ID` | ID тест-рана (если задан, ревизия получается из Kiwi, результаты отправляются в этот тест-ран) | Нет (но нужен для отправки) |
| `KIWI_REPORTING_ENABLED` | Включить/отключить отправку (по умолчанию `true`) | Нет |
| `KIWI_SKIP_IF_UNAVAILABLE` | Если `true`, при недоступности Kiwi не падать, а сохранять локально (по умолчанию `true`) | Нет |

### 2.4. Как работает получение ревизии

1. Приоритет: `BUILD_REVISION` → `--revision` → Kiwi (через `KIWI_TESTRUN_ID`).
2. Если задан `KIWI_TESTRUN_ID`, клиент Kiwi:
   - Аутентифицируется на `KIWI_URL` с логином/паролем.
   - Запрашивает тест-ран по ID через API `/api/v1/testruns/{id}/`.
   - Проверяет, что тест-ран назначен на пользователя `KIWI_USER` (поле `assignee` или `owner`).
   - Если назначение совпадает — извлекает ревизию из поля `build`.
   - Если не совпадает — возвращает `None` (ревизия не найдена).

### 2.5. Как работает отправка результатов

1. Плагин `pytest_kiwi_plugin` собирает для каждого теста:
   - ID кейса (из `src/case_ids.py`).
   - Статус (PASS/FAIL/SKIP).
   - Комментарий (сообщение об ошибке).
   - Аттачменты: скриншоты (из `SCREENSHOT_DIR`), лог (из `logs/`), diff-изображения.
2. Для каждого теста вызывается `kiwi_client.add_case_result()`.
3. После завершения всех тестов вызывается `kiwi_client.close_testrun()`.
4. Если Kiwi недоступен, результаты сохраняются локально в JSON-файл (`kiwi_results_*.json`).

### 2.6. Реализованная интеграция с Kiwi TCMS (выполнено)

**Все API-запросы в `utils/kiwi_client.py` реализованы через официальную библиотеку `tcms-api`:**
- `add_case_result()` — добавление результата для кейса (обновление статуса и комментария).
- `close_testrun()` — закрытие тест-рана (установка `stop_date`).
- `upload_attachment()` — загрузка аттачмента (через Base64).

**Карта статусов Kiwi (для корректного отображения):**
| Статус в коде | ID в Kiwi | Название в Kiwi |
|---------------|-----------|-----------------|
| `PASS`        | 4         | PASSED          |
| `FAIL`        | 5         | FAILED          |
| `ERROR`       | 7         | ERROR           |
| `SKIP`        | 6         | BLOCKED         |

**Используемый XML-RPC endpoint:** `https://<KIWI_URL>/xml-rpc/`

**Тестирование подтвердило:**
- ✅ Аутентификация через `Auth.login` работает.
- ✅ Получение тест-рана по ID работает.
- ✅ Получение списка исполнений (`TestExecution.filter`) работает.
- ✅ Обновление статуса исполнения работает.
- ✅ Проверка назначения не обязательна — можно обновлять без неё.

---

## 3. Текущее состояние проекта

### 3.1. Созданные файлы

| Файл | Назначение | Статус |
|------|------------|--------|
| `VERSION` | Версия продукта (читается при отсутствии APP_VERSION) | ✅ Готов |
| `utils/__init__.py` | Инициализация пакета утилит | ✅ Готов |
| `utils/config.py` | Получение ревизии, версии, платформы (с поддержкой Kiwi) | ✅ Готов |
| `utils/download.py` | Скачивание установщика с Jfrog с кэшированием | ✅ Готов |
| `utils/kiwi_client.py` | Клиент Kiwi TCMS (аутентификация, ревизия, отправка результатов) | ✅ Готов (реальная интеграция) |
| `utils/pytest_kiwi_plugin.py` | Плагин pytest для отправки результатов в Kiwi | ✅ Готов |
| `utils/vm_operations.py` | Копирование файлов, выполнение команд на ВМ | ⚠️ Заглушка (отложено) |
| `utils/app_operations.py` | Установка, запуск/остановка приложения | ⚠️ Заглушки (TODO) |
| `tests/conftest.py` | Фикстуры: installer_path, prepare_environment, install_app, app_session | ✅ Добавлены |
| `tests/test_install.py` | Тест установки (выполняется первым) | ⚠️ Заглушка (TODO) |
| `tests/example_test_with_app_session.py` | Пример использования app_session | ✅ Готов |
| `INSTRUCTION_FOR_AI.md` | Этот файл | ✅ Готов |

### 3.2. Фикстуры в `conftest.py`

| Фикстура | Scope | Назначение | Статус |
|----------|-------|------------|--------|
| `installer_path` | session | Скачивает установщик (или использует локальный) | ✅ Готов |
| `prepare_environment` | session | Подготавливает ВМ (копирует установщик) | ⚠️ Заглушка |
| `install_app` | function | Устанавливает приложение (только для теста установки) | ⚠️ Заглушка |
| `app_session` | function | Запускает/останавливает приложение для остальных тестов | ⚠️ Заглушка |

### 3.3. Аргументы командной строки pytest

| Аргумент | Назначение |
|----------|------------|
| `--revision` | Ревизия сборки (хэш коммита) |
| `--app-version` | Версия продукта (если не указана, читается из VERSION) |
| `--installer-path` | Локальный путь к установщику (пропускает скачивание) |
| `--case` | Запуск только тестов с указанными ID (существовал ранее) |

---

## 4. Что нужно доделать (TODO)

### 4.1. Высокий приоритет (блокирует работу)

#### a) Реализовать реальные API-запросы к Kiwi TCMS
**Файл:** `utils/kiwi_client.py`.

**Методы для реализации:**
- `add_case_result(testrun_id, case_id, status, comment, attachments)` — добавить результат для кейса.
- `close_testrun(testrun_id)` — закрыть тест-ран.
- `upload_attachment(testrun_id, case_id, file_path)` — загрузить аттачмент.

**Что нужно:** изучить документацию Kiwi TCMS API и реализовать запросы.

#### b) Реализовать функции работы с приложением
**Файл:** `utils/app_operations.py`.

- `start_app(session)` — запуск приложения через UI.
- `stop_app(session)` — закрытие приложения.
- `app_is_installed(session)` — проверка установки.
- `install_app(session)` — автоматическая установка (пока не нужна, т.к. тест установки будет ручным).

#### c) Дописать тест установки
**Файл:** `tests/test_install.py`.

Добавить реальные проверки:
- Проверить, что файлы приложения появились.
- Проверить версию.
- Проверить, что ярлык создался.
- Проверить, что приложение запускается.

#### d) Переключить существующие тесты на `app_session`
**Папки:** `tests/windows/`, `tests/Astra/`, `tests/macos/`.

Заменить `vm_session` на `app_session` во всех тестах (кроме теста установки). Убедиться, что тесты не используют глобальные переменные.

### 4.2. Средний приоритет

#### e) Добавить восстановление снапшота ВМ
**Файл:** `tests/conftest.py`, фикстура `prepare_environment`.

Сейчас там только логирование. Нужно добавить вызов API libvirt для восстановления снапшота «чистая система без приложения».

#### f) Реализовать копирование установщика на ВМ (отложено)
**Файл:** `utils/vm_operations.py`.

Пока заглушка. Будет реализовано позже, когда появится доступ к ВМ (SSH или другой способ).

### 4.3. Низкий приоритет (улучшения)

#### g) Интеграция с GitLab CI (заглушка)
Создать шаблон `.gitlab-ci.yml` с переменными.

#### h) Кэширование установщиков
Уже реализовано: файлы сохраняются в `downloads/` и используются повторно. Нужно добавить очистку старых файлов (функция `cleanup_old_cache()` уже есть, но не вызывается).

#### i) Логирование и обработка ошибок
Улучшить логирование и сделать понятные сообщения об ошибках.

---

## 5. Как использовать новую систему

### 5.1. Локальный запуск с указанием ревизии
```bash
pytest tests/ --revision=88034f7d4 --app-version=4.2.0
```

### 5.2. Использование локального установщика (без скачивания)
```bash
pytest tests/ --installer-path=/path/to/AS_4.2.0_88034f7d4_windows-x86_64.exe
```

### 5.3. Запуск только теста установки
```bash
pytest tests/test_install.py
```

### 5.4. Запуск конкретного теста с ID
```bash
pytest tests/ --case TC-85
```

### 5.5. Запуск с отправкой результатов в Kiwi
```bash
export KIWI_URL="https://kiwitcms.altami.ru"
export KIWI_USER="ваш_логин"
export KIWI_PASSWORD="ваш_пароль"
export KIWI_TESTRUN_ID=12345
export VM_ID=windows   # или astra
pytest tests/
```

### 5.6. Запуск с отключённой отправкой в Kiwi
```bash
export KIWI_REPORTING_ENABLED=false
pytest tests/ --revision=88034f7d4
```

---

## 6. Структура проекта (ключевые папки)

```
.
├── VERSION                      # Версия продукта (читается автоматически)
├── INSTRUCTION_FOR_AI.md        # Этот файл
├── config/
│   └── settings.py              # Настройки (пути, VM_ID, etc.)
├── src/
│   ├── case_ids.py              # Сопоставление тестов с ID кейсов Kiwi
│   ├── vm_manager.py            # Управление ВМ (libvirt + QMP)
│   ├── qmp_client.py            # Клиент для QEMU QMP
│   └── ...
├── tests/
│   ├── conftest.py              # Фикстуры (включая новые)
│   ├── test_install.py          # Тест установки (должен быть первым)
│   ├── example_test_with_app_session.py  # Пример использования
│   ├── windows/                 # Тесты для Windows
│   ├── Astra/                   # Тесты для Astra Linux
│   └── macos/                   # Тесты для macOS
├── utils/                       # Пакет утилит
│   ├── __init__.py
│   ├── config.py                # Получение ревизии, версии, платформы
│   ├── download.py              # Скачивание установщика с кэшированием
│   ├── kiwi_client.py           # Клиент Kiwi TCMS (API)
│   ├── pytest_kiwi_plugin.py    # Плагин pytest для Kiwi
│   ├── vm_operations.py         # Копирование файлов, выполнение команд на ВМ
│   └── app_operations.py        # Установка, запуск/остановка приложения
└── downloads/                   # Кэш установщиков (создаётся автоматически)
```

---

## 7. Зависимости (что может понадобиться)

- `requests` — для скачивания установщика и API Kiwi.
- `paramiko` — для SCP (если будете копировать через SSH).
- `pytest-order` — для управления порядком тестов.
- `pytest-asyncio` — для асинхронных фикстур.

---

## 8. Контакты и доступы

- **Jfrog (cache.altami.ru)** — логин/пароль через переменные `CACHE_USER`/`CACHE_PASSWORD`.
- **Kiwi TCMS** — `https://kiwitcms.altami.ru`, пользователь `autotest-local`.
- **ВМ** — управление через libvirt (уже настроено в `src/vm_manager.py`).

---

## 9. План на следующий сеанс (приоритеты)

### Сессия 1: Завершить интеграцию с Kiwi TCMS ✅ (ВЫПОЛНЕНО)
1. ✅ Изучить документацию API Kiwi TCMS — использована библиотека `tcms-api`.
2. ✅ Реализовать реальные API-запросы в `utils/kiwi_client.py`:
   - `add_case_result()` — через `TestExecution.update`
   - `close_testrun()` — через `TestRun.update` с `stop_date`
   - `upload_attachment()` — через `TestRun.add_attachment` (Base64)
3. ✅ Протестировать отправку результатов в реальный тест-ран (Run 24, execution 642 → PASSED).
4. ⏳ Убедиться, что аттачменты (скриншоты, лог, diff) загружаются — **будет проверено при первом полноценном прогоне**.

### Сессия 2: Реализовать функции работы с приложением
1. Реализовать `start_app()`, `stop_app()`, `app_is_installed()`.
2. Дописать тест установки.
3. Переключить существующие тесты на `app_session`.

### Сессия 3: Добавить восстановление снапшота и улучшения
1. Реализовать восстановление снапшота в `prepare_environment`.
2. Добавить очистку кэша.
3. Улучшить логирование и обработку ошибок.

---

---

## 11. Локальная разработка и режим разработчика

### 11.1. Текущие возможности локального запуска

В текущей архитектуре **уже предусмотрена** возможность локальной разработки без привязки к CI и без автоматического создания тест-ранов в Kiwi:

1. **Аргументы командной строки pytest**:
   - `--revision` – указать ревизию вручную.
   - `--app-version` – указать версию продукта.
   - `--installer-path` – использовать локальный установщик (скачивание пропускается).
   - `--case` – запустить только определённые тесты по ID.

2. **Переменные окружения для Kiwi** (опциональны):
   - `KIWI_REPORTING_ENABLED=false` – полностью отключить отправку результатов в Kiwi.
   - `KIWI_SKIP_IF_UNAVAILABLE=true` – если Kiwi недоступен, результаты сохраняются локально в JSON, а тесты не падают.

3. **Фикстуры** уже используют эти параметры и могут работать локально (скачивание установщика, подготовка ВМ – пока заглушки, но их можно доделать).

### 11.2. Режим разработчика

Для удобства локальной разработки предлагается добавить **режим разработчика** – специальный режим запуска, который:

- Автоматически отключает отправку в Kiwi (`KIWI_REPORTING_ENABLED=false`).
- Использует локальный кэш установщиков (уже реализовано в `utils/download.py`).
- Включает более подробное логирование (уровень DEBUG).
- Позволяет запускать только тесты, помеченные маркером `@pytest.mark.dev` (для быстрой итерации).

Режим разработчика активируется через аргумент `--dev` или переменную окружения `DEV_MODE=true`.

### 11.3. Скрипт-обёртка для локального запуска

Для упрощения запуска рекомендуется создать скрипт-обёртку `run_local.sh` (или `run_local.py`), который:

1. Проверяет наличие переменных окружения (ревизия, версия, путь к установщику).
2. Если не заданы – запрашивает их интерактивно.
3. Запускает `pytest` с соответствующими аргументами.

Пример такого скрипта можно добавить в корень проекта.

### 11.4. Как запускать локально прямо сейчас (без Kiwi)

```bash
# 1. Отключаем отправку в Kiwi
export KIWI_REPORTING_ENABLED=false

# 2. Указываем ревизию и версию (или используем локальный установщик)
pytest tests/ --revision=88034f7d4 --app-version=4.2.0

# Или с локальным установщиком (скачивание пропускается)
pytest tests/ --installer-path=/path/to/AS_4.2.0_88034f7d4_windows-x86_64.exe
```

Если Kiwi отключён, результаты сохраняются локально в `kiwi_results_*.json` (реализовано в `kiwi_client.py` как fallback).

### 11.5. Что нужно доделать для полноценной локальной разработки

Чтобы тесты реально запускались на локальной ВМ, необходимо:

1. **Реализовать копирование установщика на ВМ** (`copy_file_to_vm` в `utils/vm_operations.py`).
2. **Реализовать установку приложения** (`install_app` в `utils/app_operations.py`).
3. **Реализовать запуск/остановку приложения** (`start_app`, `stop_app` в `utils/app_operations.py`).

После этого фикстуры `prepare_environment`, `install_app` и `app_session` заработают, и вы сможете запускать тесты на своей локальной ВМ так же, как они будут работать в CI.

### 11.6. Приоритеты работ

1. **Интеграция с Kiwi TCMS** (реальные API-запросы) – **высший приоритет**, чтобы результаты тестов автоматически попадали в Kiwi.
2. **Реализация функций работы с ВМ и приложением** – чтобы тесты можно было запускать локально и в CI.
3. **Переключение существующих тестов на `app_session`** и дописывание теста установки.
4. **Добавление скрипта-обёртки и режима разработчика** – для удобства локальной разработки.

Такой порядок позволит сначала настроить интеграцию с Kiwi, затем обеспечить возможность локального запуска, а потом уже заниматься тестами.

---

---

## 12. Журнал сессий

### 12.1. Сессия 2026-09-11: что сделано (закоммичено)

Коммит `944dc57` — `feat: выбор тест-рана Kiwi при консольных прогонах и в меню ВМ`:

- **`src/kiwi_console.py`** (новый файл):
  - `ask_kiwi()` — диалог из двух раздельных вопросов (адрес сервера, номер рана).
    Enter = «как настроено в `.env`», `-` = локальный прогон.
  - `ask_kiwi_link()` — диалог из ОДНОГО вопроса (ссылка на ран или номер) для
    цепочки тестов. Пустой ввод = локальный прогон.
  - Обе формы принимают полную ссылку `https://kiwitcms.altami.ru/testruns/123/`
    и распаривают её на адрес и номер (регексп `_TESTRUN_URL_RE`).
  - `_check_testrun()` — проверка рана до старта (доступность, чужой assignee).
  - `describe_kiwi()` — строка «куда ушли результаты» для шапки и таблицы итогов.
- **`src/session_console.py`**: `run_console_session(vm_id, vm_name_override, case_id, kiwi_env)`;
  `kiwi_env` пробрасывается в env дочернего pytest; пометка Kiwi в шапке Panel и в таблице итогов;
  диалог задаётся один раз за сеанс.
- **`src/console_launcher.py`**: `launch_console()`/`session_command()` передают `case_id` и
  `kiwi_env` в новое окно ФЛАГАМИ (`--case`, `--kiwi-url`, `--kiwi-testrun`, `--no-kiwi`),
  чтобы значения были видны в `ps`.
- **`run_tests.py`**: флаги `--case` (можно повторять), `--no-kiwi`, `--kiwi-url`, `--kiwi-testrun`.
- **`src/dev_run.py`**: `run_single_case(..., kiwi_env=None)`.
- **`src/vm_menu.py`**: пункт 8 спрашивает тест-ран один раз на всю пачку ВМ; пункт 9
  (один тест) всегда локальный, вопрос про Kiwi не задаётся.
- **`README.md`**: разделы про интерактивное меню, локальный запуск и диалог Kiwi.

### 12.2. Договорённости по Kiwi (подтверждены)

- В консоли спрашиваем **две вещи раздельно**: адрес сервера Kiwi и номер рана.
- Можно вставить **одну строку** — полную ссылку; номер из неё выдёргивается.
- `KIWI_URL` — базовый адрес сервера, а **не** ссылка на ран; ран — числовой `KIWI_TESTRUN_ID`.
- Отдельный флаг для закрытия рана не нужен: `close_testrun()` уже вызывается в
  `pytest_sessionfinish()` безусловно.
- Для **цепочки** тестов: пустой ввод = локальный прогон (безопасный дефолт).
- Для **одиночного** теста (режим разработчика): всегда локально, вопрос не задаётся.

### 12.3. Проблема, из-за которой тесты не запустились (сессия 2026-09-11)

При запуске `python run_tests.py --menu`, пункт 8, ран `https://kiwitcms.altami.ru/runs/26/`:

```
Kiwi: ран 26 — https://cache.altami.ru/artifactory/app-builds/altami/a20e93421/windows-x86_64/AS_4.2.0_a20e93421_windows-x86_64.exe (windows)
Kiwi: kiwitcms.altami.ru, тест-ран 26
Куда пишем: kiwitcms.altami.ru, тест-ран 26
OK консоль 'gnome-terminal' открыта для ВМ 'windows'
```

Дальше прогон не пошёл: **`utils/download.py` получил HTML-страницу JFrog
(«enable JavaScript») вместо `.exe`**. Скачивание установщика на хосте падает,
потому что URL артефакта в ране 26 — не прямой бинарник (либо UI-страница
Artifactory, либо нужна аутентификация).

Это **не связано** с правками Kiwi в консоли: диалог и проброс рана работают,
падает подготовка прогона (`installer_path` → `download_installer`).

### 12.4. Что решили изменить (НОВЫЕ ТРЕБОВАНИЯ — ещё НЕ реализованы)

**Главное:** установщик больше НЕ скачивается на хосте до старта тестов.

Новая схема прогона:

1. **Низкоприоритетный тест «запуск ВМ»** — стартует ВМ и ждёт, пока загрузится ОС.
2. **Высокоприоритетный тест «скачивание установщика»** — ВНУТРИ ВМ (в гостевой ОС)
   скачивает `.exe` по URL из тест-рана Kiwi.
3. **Высокоприоритетный тест «первый запуск и настройка»** — устанавливает приложение,
   выполняет первичную настройку.
4. **Дальше — изолированные кейсы** — после настройки каждый кейс независим.

Таким образом, `download_installer()` на хосте в фикстуре `installer_path` **не нужен**;
URL передаётся в тест, а скачивание делается внутри гостя.

### 12.5. Решения пользователя по открытым вопросам

**Вопрос: объём правки?**
Ответ: см. п. 9.4 — скачивание уходит в тест внутри ВМ. Пока URL должен
игнорироваться на этапе подготовки, чтобы прогон ВМ и тестов продолжался.

**Вопрос: что с получением ревизии из Kiwi на этапе подготовки?**
Ответ: **оставить как есть — ревизия из Kiwi обязательна**
(приоритет: `BUILD_REVISION` → `--revision` → Kiwi → ошибка).

**Вопрос: что с фикстурами `prepare_environment` и `install_app`?**
Ответ: **переписать под новую схему** (ВМ → скачивание в госте → установка).
Сейчас это пустые заглушки с TODO.

**Вопрос: откуда тест внутри ВМ берёт URL установщика?**
Ответ: **брать URL напрямую из тест-рана Kiwi**
(а не собирать из ревизии через `_build_url()`).

### 12.6. ОТКРЫТЫЕ ВОПРОСЫ (задать пользователю в начале следующей сессии)

1. **Какой именно метод достаёт URL артефакта из тест-рана Kiwi?**
   В `utils/kiwi_client.py` сейчас НЕТ такого метода:
   - есть `get_testrun(testrun_id)` → возвращает dict рана;
   - есть `get_build_revision(testrun_id)` → `data['build']['name']`.
   В логе URL `.exe` пришёл из рана, но из какого поля — неизвестно. Нужно:
   - узнать имя поля (`build`? `notes`? `summary`? отдельный кастомный атрибут?);
   - проверить через `inspect_run_24.py` / `test_run_24.py` в корне проекта;
   - написать метод `get_installer_url(testrun_id)` в `KiwiClient`.

2. **Что делать, если URL в ране отсутствует или ран не задан (локальный прогон)?**
   - тест скачивания должен `skip`-нуться?
   - или падать с понятной ошибкой?
   - или использовать fallback-сборку URL из `BUILD_REVISION`?

3. **Как тест внутри ВМ будет скачивать файл?**
   - PowerShell `Invoke-WebRequest` для Windows? `curl`/`wget` для Astra?
   - нужны ли `CACHE_USER`/`CACHE_PASSWORD` внутри гостя, и как они туда попадут?
   - куда сохранять (рабочий стол? временная папка?) и как потом передавать путь тесту установки?

4. **Как связать кейсы в цепочку?** Пункт 9.4 говорит «сначала тест скачивания,
   потом тест установки». Но `pytest` по умолчанию не гарантирует порядок.
   - использовать `pytest-order` (`@pytest.mark.order(1)`, `.order(2)`)?
   - или сцеплять через фикстуры (session-scoped результат одного передаётся дальше)?
   - или вообще объединить скачивание + установку в один тест?

5. **Нужно ли убрать `--installer-path` и фикстуру `installer_path` совсем,**
   или оставить как аварийный обход (локальный файл вместо скачивания в госте)?

6. **Что с `utils/download.py`?** Оставить функцию (для аварийного/ручного
   скачивания на хосте) или удалить, раз она больше не используется в фикстурах?

7. **Кэширование в госте:** если тест скачивания перезапускается, качать заново
   или проверять, что файл уже есть в госте?

### 12.7. Файлы, которые придётся править в следующей сессии (предварительно)

- `utils/kiwi_client.py` — добавить `get_installer_url(testrun_id)`.
- `tests/conftest.py` — переписать `installer_path`, `prepare_environment`, `install_app`.
- `tests/test_install.py` — переписать под новую схему (скачивание в госте + установка).
- `utils/vm_operations.py` — `copy_file_to_vm` больше не нужен для установщика,
  но может понадобиться для других файлов; решить судьбу.
- `utils/download.py` — решить: оставить или удалить.
- `utils/config.py` — `get_revision()` остаётся обязательным (см. п. 9.5).
- `README.md` / `INSTRUCTION_FOR_AI.md` — обновить после реализации.

### 12.8. Важное про окружение

- `.env` содержит `KIWI_URL`, `KIWI_USER`, `KIWI_PASSWORD`, но **НЕ содержит
  `KIWI_TESTRUN_ID`**. Значит, Enter в диалоге `ask_kiwi()` даст локальный прогон,
  пока номер рана не введут руками или не добавят в `.env`.
- Python в проекте — `venv/bin/python` (3.12). Системный `python3` не видит
  `dotenv`/`libvirt`, поэтому запускать только через venv.
- Запуск меню: `source venv/bin/activate && python run_tests.py --menu`.

---

*Создано: 2026-09-04*
*Последнее обновление: 2026-09-11*
